{
  "name": "@wayv-software/mcptap",
  "version": "0.5.1",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WayV-Inc/mcptap.git"
  },
  "homepage": "https://github.com/WayV-Inc/mcptap#readme",
  "description": "Audit proxy for MCP servers — see and log every tool call your AI agents make",
  "type": "module",
  "bin": {
    "mcptap": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node test/smoke.js && node test/setup.test.js && node test/add.test.js && node test/tools.test.js && node test/ui.test.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "audit",
    "security",
    "ai-agents",
    "logging",
    "proxy"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
